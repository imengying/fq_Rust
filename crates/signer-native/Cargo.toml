[package]
name = "fq-signer-native"
version = "0.1.0"
edition = "2021"

[lib]
name = "fq_signer_native"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
emulator = { path = "../../vendor/rnidbg/emulator", default-features = false, features = ["unicorn_backend"] }
log = "0.4"
