[package]
name = "fq-signer-native"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
base64 = "0.22"
bytes = "1"
emulator = { path = "../third_party/rnidbg/emulator", default-features = false, features = ["unicorn_backend"] }
env_logger = "0.11"
log = "0.4"
once_cell = "1"

